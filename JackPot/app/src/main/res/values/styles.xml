<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/roboto_font</item>
        <item name="android:actionMenuTextAppearance">@style/menu_text</item>
    </style>

    <style name="EditTextTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">@color/colorLightGray</item>
        <item name="colorControlActivated">@color/colorButtonSelect</item>
        <item name="colorControlHighlight">@color/colorButtonSelect</item>
        <item name="android:fontFamily">@font/roboto_font</item>
    </style>

    <!--splash 화면에 사용될 테마-->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

    <!-- 메뉴바 텍스트 관련 -->
    <style name="menu_text" parent="TextAppearance.AppCompat.Widget.ActionBar.Menu">
        <item name="android:textSize">12dp</item>
    </style>

</resources>